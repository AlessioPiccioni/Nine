package com.piccionialessio.nine.model

import java.time.LocalDate
import java.util.Date

class Game {
    var permutation = Array<Int>(9, { it -> 0 })
    val date: LocalDate = LocalDate.now()
    /*tempo
    esito
    nome
     */
}